package oops_polymorphism;

public class PolymorphismTheory {
	/*
	 * 🔄 POLYMORPHISM IN JAVA - THEORY NOTES 🔄
	 *
	 * 1. What is Polymorphism?
	 * -------------------------
	 * ➤ Polymorphism means "many forms".
	 * ➤ It allows objects to perform different behaviors (methods) through a common interface or name.
	 * ➤ In Java, polymorphism is mainly achieved through:
	 *      - Method Overloading (Compile-time)
	 *      - Method Overriding (Runtime)
	 *
	 *
	 * 2. Types of Polymorphism:
	 * --------------------------
	 * ✅ Compile-time Polymorphism (Static Binding)
	 *    ➤ Achieved using Method Overloading.
	 *    ➤ Method has the same name but different parameters (number/type/order).
	 *
	 * ✅ Runtime Polymorphism (Dynamic Binding)
	 *    ➤ Achieved using Method Overriding.
	 *    ➤ A subclass provides a specific implementation of a method already defined in its superclass.
	 *    ➤ The method to be executed is determined during runtime based on the actual object.
	 *
	 *
	 * 3. Advantages of Polymorphism:
	 * -------------------------------
	 * ✔ Code reusability and flexibility
	 * ✔ Method behavior can vary at runtime
	 * ✔ Clean and maintainable code
	 * ✔ Supports dynamic method dispatch
	 * ✔ Essential for abstraction and interface-based design
	 *
	 *
	 * 🔚 Summary:
	 * -------------------------------
	 * ➤ Polymorphism allows one interface to be used for a general class of actions.
	 * ➤ The actual action is determined by the specific object at runtime (or overload at compile time).
	 * ➤ A key principle of OOP along with inheritance, encapsulation, and abstraction.
	 */

}
